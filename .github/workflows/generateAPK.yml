name: Build APK

on:
  push:
    branches:
      - main  # Cambiar por el nombre de tu rama principal

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2
      
    - name: Setup JDK
      uses: actions/setup-java@v2
      with:
        java-version: '11'  # Cambiar por la versión de Java que necesites
      
    - name: Build with Gradle
      run: .flutter build apk  # O el comando que utilices para construir tu APK
      
    - name: Upload APK
      uses: actions/upload-artifact@v2
      with:
        name: WealthWars
        path: app/build/outputs/apk/debug/WealthWars.apk  # Ruta al APK generado